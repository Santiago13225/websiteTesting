function ListAvgExc(arrayParams) {

    let sum = 0;
    for (let i = 0; i < arrayParams.length; i++) {
      sum += arrayParams[i];
      // console.log("Avg Hours of Exercise: ");
    }
    let avgExc = sum/(arrayParams.length);
    return avgExc
  }

module.exports = ListAvgExc